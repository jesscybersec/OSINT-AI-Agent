# Sample Reports

This folder contains public, non-sensitive sample reports that demonstrate the output format of the OSINT AI Agent.

Purpose:

- show report structure
- illustrate profile-based output
- provide safe examples for GitHub visitors

Notes:

- real execution output generated during local investigations should stay in `reports/`
- `reports/` remains ignored for `.md` files by default
- only curated example reports should be committed here
